[*This program finds the area and checks if it is less then or greater than the max value*]
$
[* NO function definitions *]
$
integer area_x, area_y, max_value; boolean yes, no; [* declarations *]
$
{
    not_declared = 10;
    max_value = 20;
    scan (area_x, area_y);
    {
        if (length * max_value < width) {
            yes = true;
        } else {
            no = true;
        }
    }
}
if (no == true) {
    print(no);
    } else {
        print(yes);
    }
$