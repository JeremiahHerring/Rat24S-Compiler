$
$
integer low, high, step; [* declarations *]
$
scan (low, high, step);
while (low <= high )
{ 
    print (low);
    print ((low, width));   [* Calculate area with given length and width *]
    low = low + step;
}
endwhile
while (low <= high )
{ 
    print (low);
    print ((low, width));   [* Calculate area with given length and width *]
    low = low + step;
}
endwhile
$