[* this is sample test for <compound> *]
$
[* NO function definitions *]
$
integer i, max, sum, i; boolean yes, no; [* declarations *]
$
{
    not_declared = 10
    sum = 0;
    i = 1;
    scan (max);
    {
        while (i < max) {
            sum = sum + i;
            i = i + 1;
        } endwhile
    }
}
{
    no = false;
    scan(yes);
    if (yes == no) {
       max = yes + no;
       no = 10; 
       yes = yes + 20
    } endif
}
print(no);
print (yes  * true);
$