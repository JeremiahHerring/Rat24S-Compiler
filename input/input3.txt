[* this is sample test for <compound> *]
$
[* NO function definitions *]
$
integer i, max, sum; boolean yes, no; [* declarations *]
$
{
    sum = 0;
    i = 1;
    scan (max);
    {
        while (i < max) {
            sum = sum + i;
            i = i + 1;
        } endwhile
    }
}
{
    no = false;
    scan(yes);
    if (yes == no) {
       max = yes + no;
       no = true; 
    } endif
}
print(no);
print (sum + max);
$